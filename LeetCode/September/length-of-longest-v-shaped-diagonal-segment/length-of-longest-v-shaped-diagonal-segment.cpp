
#include <iostream>
#include <vector>
#include <string>
#include <chrono>
#include <algorithm>
using namespace std;
using namespace chrono;

int lenOfVDiagonal(vector<vector<int>> &grid)
{
    int row = 0;
    int col = 0;
    bool isUsed = false;
    bool toContinue = true;
    int length = 0;
    while (toContinue)
    {
        
    };
}

int main()
{
    vector<vector<int>> v = {{2, 2, 1, 2, 2}, {2, 0, 2, 2, 0}, {2, 0, 1, 1, 0}, {1, 0, 2, 2, 2}, {2, 0, 0, 2, 2}};

    return 0;
}